# Changelog

## [Unreleased]

## [0.8.0] - 2026-08-11

### 호환성 변경

- Chrome extension과 Side Panel 지원을 제거하고 **local Web App**을 유일한 사용자 화면으로
  통합했습니다. Setup launcher는 extension 설치나 ID 입력 없이 Web App을 준비하며, start launcher가
  기본 browser에서 `/app/`을 엽니다.
- 기존 Chrome extension 사용자는 Chrome의 확장 프로그램 관리 화면에서 기존 도구를 제거한 뒤 platform
  setup launcher를 다시 실행해 수동으로 전환해야 합니다. Extension의 browser-local 설정은 자동
  이전되지 않으므로 Web App의 **관리 > 설정**에서 기본값·자동화 동의·안전 한도를 다시 확인하세요.
  Local service의 SQLite 데이터는 extension을 제거해도 삭제되지 않습니다.
- GitHub Release에서 extension ZIP을 제거했습니다. 이제 Web App을 포함한 Python wheel과 checksum만
  제공합니다.
- Extension Origin을 허용하던 호환 경로를 제거했습니다. Web App 요청은 기본적으로 service와 같은
  origin에서만 허용되며, 신뢰하는 private Wi-Fi에서 명시적으로 pairing한 기기만 제한된 session으로
  접근할 수 있습니다.

### 개선

- Naver page probe와 packaged E2E를 `client/` toolchain으로 모아 Web App의 build·test 흐름을 하나로
  통합했습니다.
- Client의 statements·branches·functions·lines coverage 기준을 각각 85% 이상으로 높였습니다. 화면 이동
  뒤의 오래된 응답, 글쓰기 autosave·checkpoint·staging 경쟁 상태, 설정 저장과 session 취소·재시도처럼
  사용자 입력 유실이나 중복 실행으로 이어질 수 있는 회귀 테스트를 강화했습니다.

## [0.7.0]

### 추가

- **홈**을 작업 중심 대시보드로 바꿔 오늘의 수집 요약, 준비 상태, 다음 행동과 빠른 시작을 한곳에서
  확인할 수 있습니다.
- 첫 실행에는 앱 준비, AI 연결, 내 블로그, 자동화 브라우저, 네이버 로그인, 안전 설정을 순서대로
  안내하는 단계형 초기 설정을 제공합니다. 현재 필요한 설정 화면이나 브라우저 동작으로 바로 이동할
  수 있습니다.
- **글쓰기**를 집중형 편집 공간으로 재구성했습니다. 제목과 block 본문을 중심에 두고 최근 초안,
  AI 도구, 이미지, 태그, 변경 기록, 임시저장을 필요한 때만 펼쳐 사용할 수 있습니다.

### 개선

- 공통 앱 셸과 responsive 화면 체계를 정리해 데스크톱·태블릿·좁은 화면에서도 홈·작업함·글쓰기·관리
  사이를 같은 방식으로 이동하고, keyboard와 screen reader로 현재 화면을 확인할 수 있습니다.
- **작업함**을 목록 중심으로 단순화했습니다. 출처·상태·검색·정렬을 한곳에서 조절하고, 선택한 글의
  상세와 일괄 처리 범위를 이어서 확인할 수 있으며, 좁은 화면에서는 필요한 상세만 열어 볼 수 있습니다.
- 글쓰기 편집 중 제목과 block은 working copy로 자동 저장하고, `버전으로 남기기`를 명시적으로 눌렀을
  때만 revision을 남깁니다. 빈 제목·빈 문단을 임시저장하지 않으며, AI·태그 도구와 네이버 임시저장
  전제 조건을 화면에서 안내합니다.
- **관리 > 설정**을 작업 기본값, 탐색 및 자동화, 연결 및 앱으로 나누고, 자주 쓰는 설정은 바로
  보여 주며 안전 한도·예약·AI 예산·연결 세부값과 데이터 초기화는 접어 둘 수 있게 했습니다.
- 태블릿과 휴대폰에서 선택 항목을 누르기 쉽도록 touch target과 좁은 화면의 정보 순서를 보강했습니다.
- 세션 배치의 실패·승인·재시도 경계를 중심으로 품질 검증을 강화하고 Python 품질 기준을 branch
  coverage 87% 이상으로 높였습니다. 설치·운영·사용자 안내도 현재 `관리` 메뉴와 실제 작업 흐름에
  맞춰 정리했습니다.

### 수정

- 화면을 이동하거나 다른 글을 여는 동안 도착한 이전 요청의 늦은 응답이 현재 화면·작업함·초안을
  덮어쓰지 않도록 했습니다. 현재 경로와 대상에 해당하는 최신 상태만 반영합니다.
- 로딩·새로고침 중 작업함 선택과 세션 진행 상태가 이전 화면으로 되돌아가거나 잘못된 세션을 닫지
  않도록 보강했습니다.
- 설정 저장·재시작·비동기 갱신 중에도 입력, 열린 세부 패널, focus와 선택 상태가 조용히 사라지지
  않도록 수정했습니다.

## [0.6.0]

### 추가

- 홈·작업함·글쓰기·더보기의 네 화면으로 웹앱 navigation을 정리하고, 데스크톱과 태블릿에서 이어서
  작업할 수 있는 responsive workbench를 제공했습니다.
- 문단·소제목·인용·목록·구분선·이미지 블록을 편집하는 네이버 스타일 writing studio와
  working copy 자동저장을 추가했습니다.
- AI·Naver Search·SMTP·브라우저 연결을 PC 전용 보호 설정에서 관리하고, 설정 적용을 명시적으로
  재시작하는 runtime configuration 흐름을 추가했습니다.

### 개선

- 작업함에 source·검색어·작성자·시각 맥락, cursor pagination, 보류 글 복구, 일괄 처리 안전 확인을
  제공해 긴 목록에서도 현재 작업 위치를 잃지 않도록 했습니다.
- 네이버 임시저장은 지원하는 block 구조와 순서를 검증하며, 구조를 확인할 수 없을 때 평문으로
  조용히 변환하지 않고 안전하게 중단하도록 했습니다.
- standalone PWA shell, onboarding 준비 카드, activity 요약 카드와 tablet detail sheet를
  추가해 재개 흐름과 상태 확인을 단순화했습니다.

### 수정

- 댓글 recommendation 응답에 없는 `version` 필드를 client fixture와 parser가 임의로 기대하던
  계약 불일치를 제거했습니다.
- queue를 새로고침하는 동안 상세 action이 조용히 무시되던 race를 막고, 완료 후에만 보류 복구를
  실행하도록 했습니다.

## [0.5.6]

### 수정

- 댓글이 실제로 등록된 뒤에도 네이버의 0×0 Captcha placeholder를 제한으로 오인하던 문제를
  수정했습니다. 댓글 등록 확인을 먼저 하고, 화면에 실제로 표시된 Captcha만 중단 사유로 처리합니다.
- 네이버 댓글 입력란과 등록 버튼이 형제 영역으로 나뉜 글에서 등록 버튼을 찾지 못하던 문제를
  수정했습니다. 이제 공통 `.u_cbox_write_wrap` 또는 form 범위에서 등록 버튼을 정확히 찾습니다.
- 서로이웃 신청은 현재 탭만 보지 않고 Naver `BuddyAdd` popup을 찾아 1단계 **서로이웃** 선택부터
  2단계 신청 메시지·선택된 그룹·완료 **닫기**까지 같은 popup 탭에서 이어서 확인합니다. text가 없는
  Naver close control도 class·title·aria-label로 찾아 실제로 클릭합니다. 완료가 확인된 별도 popup은
  원문 글 탭을 건드리지 않고 확장 프로그램이 닫아 남지 않게 합니다.
- 실제 Naver DOM에서 확인한 공감 2단계, 댓글 등록 영역, 서로이웃 완료 popup 구조를 sanitized fixture와
  회귀 테스트로 보관해 selector 변경을 반복 검증할 수 있게 했습니다.
- **오늘의 작업**의 `서로이웃 신청 계속하기`가 저장된 원문 후보 글을 현재 탭에 다시 열고
  댓글 작성 흐름을 준비하도록 수정했습니다. 더 이상 페이지를 직접 찾아갈 필요가 없습니다.
- 신규 이웃 검색 후보는 저장 검색어의 모든 단어가 제목에 포함될 때만 표시하고, 후보별 검색어 출처를
  함께 표시합니다. 삭제된 검색어 또는 더 이상 일치하지 않는 기존 후보는 local storage에 보존하되
  활성 대기열에서는 숨깁니다.

## [0.5.5]

### 수정

- 후보별 댓글 입력·재입력 버튼을 없애고, 선택한 댓글을 다듬은 뒤 한 번의 실행 버튼으로 자동 교류를
  시작하도록 정리했습니다. 수동 등록은 복사·붙여넣기로 명확히 분리했습니다.
- 별도의 **이 한 건 실행** 확인 화면을 제거했습니다. versioned 동의가 켜진 상태에서 글별 실행 버튼을
  누르는 한 번의 명시적 동작으로만 실행 token을 발급합니다.
- 수동 댓글 등록 뒤 서로이웃 신청만 남은 신규 후보를 **오늘의 작업**에서 `서로이웃 신청 대기`로
  표시합니다.
- 실제 Naver popup에서 숨겨진 서로이웃 radio, 이웃 그룹 선택, 신청 메시지, 두 번의 다음과 완료 뒤
  닫기까지 처리하도록 보강했습니다.

## [0.5.4]

### 수정

- 여러 저장 검색어의 신규 이웃 후보 수를 합산할 때 50개를 넘으면 동기화 응답이 500으로 실패하던
  문제를 고쳤습니다. 검색어별 수집 제한은 유지하면서 전체 동기화 합계는 정상 표시합니다.

## [0.5.3]

### 수정

- **댓글 사용하기**로 이미 넣어 둔 선택 댓글이 승인 등록 단계에서 기존 입력으로 오인되어 중단되던
  문제를 고쳤습니다. 승인한 text가 정확히 같을 때만 그대로 등록 버튼으로 이어집니다.
- 댓글을 수동 등록으로 기록해도 신규 이웃 후보의 아직 대기인 서로이웃 신청을 건너뛰거나 전체 작업을
  종료하지 않습니다. 다시 확인·승인하면 공감·댓글은 반복하지 않고 서로이웃 신청만 이어서 실행합니다.

## [0.5.2]

### 수정

- 실제 네이버 Blog의 중복 공감 control 가운데 게시글 본문의 표준 하트만 선택하고, 반응 레이어가
  열리는 경우 기본 공감 항목까지 선택한 뒤 상태 변화를 확인합니다.
- 숨은 이웃추가 control을 제외하고, `buddyFrm → buddyApplyFrm → 완료/닫기` 네이버 popup 흐름에서
  기본 이웃이 아닌 서로이웃만 선택하도록 보강했습니다.
- 실제 구조를 닮은 sanitised fixture와 browser test를 추가해 공감·서로이웃 popup의 중복·단계 전환을
  검증합니다.

## [0.5.1]

### 추가

- 사용자가 마지막 확인 화면에서 승인한 글 한 건에 한해 공감 상태 확인, 선택 댓글 등록과
  선택적 서로이웃 신청을 순서대로 실행할 수 있습니다.
- 공감·댓글·서로이웃 단계별 결과와 성공·중단·확인 필요 상태를 로컬에 기록하고, 성공한 단계를
  재시도에서 반복하지 않습니다.
- 자동 실행 범위를 설명하는 versioned 동의와 글·댓글·실행 단계·신청 메시지를 다시 확인하는
  일회성 승인을 추가했습니다.

### 개선

- Side Panel을 **오늘의 작업**, **댓글 작성**, **최근 작업**, **설정**으로 나눠 긴 스크롤을
  줄이고, 대기 글 수·현재 작업·다음 글 이동을 한 흐름으로 정리했습니다.
- 이웃 글과 신규 이웃 후보를 현재 탭 또는 새 탭에서 처리할 수 있으며, 신규 후보의 기본
  서로이웃 신청 메시지를 local storage에 저장할 수 있습니다.
- desktop·mobile Naver URL 형식이 달라도 같은 `blogId`와 `logNo`를 가진 글을 동일하게
  연결합니다.

### 수정

- 이미 공감했거나 서로이웃·신청 대기 상태인 경우 외부 동작을 반복하지 않습니다.
- 댓글 등록이나 서로이웃 신청 결과를 확인하지 못한 경우 자동 재시도하지 않고 **확인 필요**로
  중단합니다.
- production System E2E가 이웃 성공, 신규 후보 성공, partial·unconfirmed 복구와 360px 작업
  공간 전환을 검증합니다.
- 실제 네이버 Blog의 `u_likeit_button _face` 공감 control과 접힌 댓글 영역을 인식하도록 보강하고,
  숨겨진 중복 control은 제외한 뒤 클릭·입력 결과를 다시 확인합니다.
- 실패한 교류 run에서 사용자가 직접 처리한 단계를 기록하면 댓글 추천, 탐색 대기열과
  **오늘의 작업** 상태를 함께 완료 처리합니다. 저장 검색어를 삭제해도 기존 후보 글은 유지합니다.

## [0.5.0]

### 추가

- 공식 Naver Search API 기반의 **신규 이웃 찾기**를 추가했습니다. 같은 블로그의 중복 후보와 내
  블로그·저장 이웃·최근 완료 후보는 제외하며, 검색어를 저장하면 즉시 최신 후보를 갱신합니다.
- 탐색 대기열에서 현재 글을 유지할 수 있는 **새 탭 열기**를 지원합니다. 페이지 로딩이 끝난 뒤에만
  본문 preview를 새 글로 갱신합니다.

### 개선

- Side Panel의 자동 탐색 설정을 기본 접힘인 **탐색 설정과 알림**으로 정리해, 먼저 처리할 이웃 새 글과
  신규 이웃 후보를 더 빠르게 확인할 수 있습니다.
- 검색 API credential·대기열 탐색 상태의 안내를 보강해, 필요한 설정과 실패 원인을 더 쉽게 확인할 수
  있습니다.

## [0.4.0]

### 추가

- **글 탐색 대기열**을 추가했습니다. 등록 이웃의 새 글과 저장 검색어에서 가져온 신규 이웃 후보를
  분리해 보고, 사용자가 확인한 뒤 직접 글을 열 수 있습니다.
- 열린 네이버 이웃 목록에서 블로그를 가져오거나 URL·ID를 직접 등록할 수 있습니다. 저장 검색어와
  제외어를 관리하고, 사용자가 연 검색 결과에서 후보 글을 최대 50개까지 가져올 수 있습니다.
- 이웃 블로그의 공개 RSS를 수동 또는 하루 한 번 갱신하며, 대기열 badge·Chrome 알림과 generic SMTP
  이메일 요약을 opt-in으로 지원합니다.

### 수정

- 탐색 데이터는 제목·URL·게시 시각·출처·상태만 로컬 SQLite에 보관하고, 본문·쿠키·로그인 정보는
  수집하거나 저장하지 않습니다.

## [0.3.0]

### 추가

- 최근 완료 댓글 최대 5개를 스타일 예시로 활용하는 개인화 옵션을 추가했습니다. 이 옵션은 기본으로
  켜져 있으며, 생성 결과에서 실제 활용 개수를 확인할 수 있습니다.
- 최근 작업에서 완료 댓글별 스타일 예시 포함·제외와 전체 예시 정리를 지원합니다. 기록 자체는 유지됩니다.
- 승인 후 댓글 입력이 실패했을 때, API를 다시 호출하지 않고 같은 승인 댓글을 다시 입력하는 버튼을
  추가했습니다.

### 수정

- 네이버의 `contenteditable` 댓글 입력란과 표준 **댓글쓰기** 버튼 탐색을 보강해, 열린 입력란을
  찾지 못하는 경우의 안전한 재시도와 복사 fallback을 개선했습니다.
- 개인화 설정을 생성 요청의 멱등성 정책에 포함하고, 로컬 SQLite와 extension 설정 migration을
  추가했습니다.

## [0.2.0]

### 추가

- 닫힌 네이버 댓글 입력란에서 표준 **댓글쓰기** 버튼을 열고 초안을 입력할 수 있습니다.
- native Linux와 Ubuntu WSL에서 `setup-linux.sh`, `start-linux.sh`로 간편 설정과 실행을 지원합니다.
- stable version tag를 push하면 Python wheel과 Chrome extension ZIP을 포함한 GitHub Release를 자동으로
  게시합니다.

### 수정

- 네이버의 `contenteditable` 댓글 입력란을 인식해, 이미 열린 입력란을 찾지 못하던 문제를 해결했습니다.

## [0.1.0]

### 추가

- Human-in-the-loop 네이버 블로그 댓글 작성 보조 도구의 첫 공개 버전입니다.
