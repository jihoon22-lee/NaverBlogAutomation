#!/usr/bin/env bash

set -euo pipefail

while read -r _local_ref _local_sha remote_ref _remote_sha; do
    if [[ "$remote_ref" == "refs/heads/main" ]]; then
        echo "Direct pushes to main are disabled. Push a task branch and open a pull request." >&2
        exit 1
    fi
done

